# Story 2.1: Regex-Based Signal Parser for Standard Formats

## Status
Done

## Story
**As a** trader,  
**I want** standard French trading signals to be parsed instantly,  
**so that** common signal formats are processed without AI overhead.

## Acceptance Criteria
1. Regex patterns match standard formats: "JE BUY GOLD 3362", "BUY GOLD @ 3362", "ACHETER OR 3362"
2. Parser extracts: action (BUY/SELL), instrument (GOLD/XAUUSD/OR), entry price, SL, TP when present
3. Handles variations: "JE VEND", "SELL", "SHORT", "VENDRE" for sell signals
4. Validates extracted values: prices must be realistic GOLD ranges (3000-4000), SL/TP logic verified
5. Returns structured signal object: {action, symbol, entry, sl, tp, original_message, confidence_score}
6. Logs all parse attempts with success/failure reasons for debugging

## Tasks / Subtasks
- [x] **Task 1: Create regex pattern definitions** (AC: 1, 3)
  - [x] Define French action patterns: "JE BUY", "BUY", "ACHETER", "JE VEND", "SELL", "SHORT", "VENDRE"
  - [x] Create instrument patterns: "GOLD", "XAUUSD", "OR" variations
  - [x] Build price extraction patterns with @ symbol and space variants
  - [x] Add SL/TP optional patterns for when present in message
- [x] **Task 2: Implement RegexParser class** (AC: 2, 5)
  - [x] Create `src/signal_parser/regex_parser.py` with RegexParser class
  - [x] Implement `parse(text: str) -> Optional[ParsedSignal]` method
  - [x] Add pattern matching logic with prioritized pattern attempts
  - [x] Generate structured ParsedSignal object with confidence_score=0.95
- [x] **Task 3: Add price validation system** (AC: 4)
  - [x] Create validation rules for GOLD price ranges (3000-4000)
  - [x] Implement SL/TP logic validation (SL below entry for BUY, above for SELL)
  - [x] Add minimum/maximum pip distance checks for SL/TP
  - [x] Return ValidationResult with pass/fail and specific failure reasons
- [x] **Task 4: Implement comprehensive logging** (AC: 6)
  - [x] Add structured logging for all parse attempts with correlation_id
  - [x] Log successful parses with extracted values and confidence
  - [x] Log failed parses with specific failure reasons and patterns attempted
  - [x] Include original message text (sanitized) for debugging
- [x] **Task 5: Create unit tests for regex parser**
  - [x] Test all French signal format variations from AC 1 and 3
  - [x] Test price extraction edge cases and malformed inputs
  - [x] Test validation logic with valid/invalid price ranges
  - [x] Test logging output format and content verification

## Dev Notes

### Previous Story Insights
From Story 1.4: Health monitoring and logging system is complete with structured JSON logging using correlation IDs and context tracking. All components use the logging system instead of print() statements. Error handling patterns are established with circuit breaker decorators for external API calls.

From Stories 1.1-1.3: Telegram client infrastructure is complete with TelegramClient wrapper, MessageHandler, and proper session management. Rate limiting and human-like delays are implemented. Message queue infrastructure exists but may need extension for signal processing pipeline.

### Technology Stack Requirements
**Source:** [Source: architecture/tech-stack.md]
**Critical Dependencies:**
- **Python re module** - Built-in: Regex pattern matching for signal parsing
- **Python logging** - Built-in: Structured logging already configured from Story 1.4
- **orjson 3.10.12** - Fast JSON operations for structured signal objects
- **pendulum 3.0.0** - Timezone-aware dates for signal timestamps
- **asyncio** - Built-in: Async parsing operations

### Data Models & Signal Structure
**Source:** [Source: architecture/data-models.md]
**ParsedSignal Object Structure Required:**
```python
@dataclass
class ParsedSignal:
    id: Optional[int] = None
    telegram_message_id: int
    telegram_chat_id: int
    sender: str  # Hashed for privacy
    timestamp: datetime
    raw_text: str
    parsed_action: Enum  # BUY/SELL/CLOSE/MODIFY
    symbol: str  # GOLD/XAUUSD/OR normalized
    entry_price: Decimal
    stop_loss: Optional[Decimal] = None
    take_profit: Optional[Decimal] = None
    confidence_score: float  # 0.95 for regex parser
    parser_type: str = "REGEX"
    status: str = "PENDING"
```

### Signal Parser Component Specifications
**Source:** [Source: architecture/components.md#signal-parser-component]
**Required Interfaces:**
- `parse_signal(text: str) -> ParsedSignal` - Main parsing interface
- `try_regex_parse(text: str) -> Optional[ParsedSignal]` - Fast regex path (THIS STORY)
- `try_llm_parse(text: str, context: str) -> ParsedSignal` - Fallback (Future Story 2.2)
- `validate_signal(signal: ParsedSignal) -> ValidationResult` - Sanity checks

**Integration Points:** Must integrate with Message Queue Manager as consumer, provide output to validation stage

### French Signal Pattern Requirements
**Key Patterns to Handle:**
- **Buy Actions:** "JE BUY", "BUY", "ACHETER", "J'ACHÈTE"
- **Sell Actions:** "JE VEND", "SELL", "SHORT", "VENDRE", "JE VENDS"
- **Instruments:** "GOLD", "XAUUSD", "OR" (French for gold)
- **Price Formats:** "3362", "@ 3362", "GOLD 3362", "GOLD @ 3362.50"
- **SL/TP Formats:** "SL 3350", "TP 3375", "STOP 3350", "TARGET 3375"

### Project Structure & File Locations
**Source:** [Source: architecture/source-tree.md]
**Key Implementation Files:**
- **Main Parser:** `src/signal_parser/regex_parser.py` - RegexParser class
- **Pattern Definitions:** `src/signal_parser/patterns.py` - French signal patterns
- **Data Models:** `src/signal_parser/__init__.py` - ParsedSignal dataclass
- **Test Files:** `tests/unit/test_regex_parser.py` - Comprehensive unit tests
- **Pattern Tests:** `tests/unit/test_signal_patterns.py` - Pattern matching tests

### Coding Standards Requirements
**Source:** [Source: architecture/coding-standards.md]
**Critical Rules for This Story:**
- **Never use print() for output - use logger:** All parsing output must use structured logging system
- **All external API calls must use circuit breaker decorator:** N/A for this story (no external APIs)
- **Never log sensitive data:** Hash telegram usernames, sanitize message content in logs
- **All async functions must handle cancellation:** Parser operations must cleanup on cancel
- **Database operations must use repository pattern:** N/A for this story (no direct DB operations)

### GOLD Trading Specifications
**Price Validation Rules:**
- **Valid GOLD Range:** 3000.00 - 4000.00 (adjust if market moves significantly)
- **Minimum SL Distance:** 5 pips (0.05 for GOLD)
- **Maximum SL Distance:** 500 pips (5.00 for GOLD) - risk management
- **Minimum TP Distance:** 10 pips (0.10 for GOLD)
- **SL Logic:** For BUY orders, SL must be below entry; for SELL orders, SL must be above entry

### Structured Logging Requirements
**Source:** [Source: architecture/coding-standards.md, Story 1.4 implementation]
**JSON Log Format for Parser:**
```json
{
  "timestamp": "ISO8601",
  "level": "INFO/ERROR", 
  "component": "RegexParser",
  "message": "Signal parsed successfully / Parse failed",
  "context": {
    "correlation_id": "UUID per message lifecycle",
    "service_context": "RegexParser.parse:line_number",
    "user_context": "sender_hash, chat_id"
  },
  "signal_data": {
    "action": "BUY/SELL",
    "symbol": "GOLD",
    "entry": "3362.50",
    "confidence": "0.95",
    "parser": "REGEX",
    "raw_text_hash": "SHA256 of original text"
  }
}
```

### Integration with Existing Systems
**Components to Integrate:**
- **Message Queue Manager:** Consume from raw message queue (from components.md)
- **Logging System:** Use existing structured logging from Story 1.4
- **Validation Pipeline:** Output to signal validation system (Future Story 2.3)

### Required Environment Variables
**Source:** [Source: architecture/source-tree.md, architecture/tech-stack.md]
**No new environment variables required for this story.** Uses existing logging and configuration from previous stories.

## Testing

### Testing Standards
**Source:** [Source: architecture/test-strategy-and-standards.md]
- **Framework:** pytest 8.4.1 with pytest-asyncio 0.25.0
- **File Convention:** `test_{module_name}.py`
- **Unit Test Location:** `tests/unit/`
- **Coverage Goal:** 95% minimum for parser components
- **Mocking:** unittest.mock + pytest-mock for external dependencies

### Required Test Files for This Story:
- `tests/unit/test_regex_parser.py` - Test regex pattern matching and parsing logic
- `tests/unit/test_signal_patterns.py` - Test French pattern definitions and variations
- `tests/unit/test_signal_validation.py` - Test price validation and SL/TP logic

### Test Requirements:
- **Test all French signal variations** from AC 1 and 3 with comprehensive examples
- **Test price extraction** including edge cases: no spaces, @ symbols, decimal places
- **Test validation logic** with valid/invalid GOLD prices, SL/TP distance checks
- **Test logging output** verify structured JSON format and required fields
- **Mock external dependencies** (none for this story, pure logic testing)
- **Edge case coverage** malformed messages, missing data, boundary conditions
- **Follow AAA pattern** (Arrange, Act, Assert) with clear test case descriptions

### Test Data Strategy:
**Source:** [Source: architecture/test-strategy-and-standards.md]
- **Fixtures:** `tests/fixtures/sample_signals.json` with real French signal examples
- **Factory Pattern:** Dynamic test signal generation for property-based testing
- **Cleanup:** Automatic cleanup after each test, no test pollution

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-08-12 | 1.0 | Initial story creation | sm (Bob) |

## Dev Agent Record
*This section will be populated by the development agent during implementation*

### Agent Model Used
claude-sonnet-4-20250514

### Debug Log References

### Completion Notes List
- **Task 1**: Created comprehensive French signal regex patterns in `src/signal_parser/patterns.py` with support for all required action phrases (JE BUY, ACHETER, VENDRE, etc), instrument variations (OR, GOLD, XAUUSD), and price formats (@ symbol variants)
- **Task 2**: Implemented RegexParser class with main pattern matching and component-based fallback parsing, generating structured ParsedSignal objects with 0.95 confidence for main patterns and 0.90 for component parsing
- **Task 3**: Integrated comprehensive validation system for GOLD price ranges (3000-4000), SL/TP logic validation, and pip distance checks with appropriate warnings
- **Task 4**: Implemented structured JSON logging with correlation IDs, contextual information, sanitized text hashing, and detailed success/failure reporting
- **Task 5**: Created comprehensive test suite with 40 tests covering all French signal variations, edge cases, validation logic, and pattern matching

### File List
- `src/signal_parser/__init__.py` - ParsedSignal and ValidationResult dataclasses, ParsedAction enum
- `src/signal_parser/patterns.py` - FrenchSignalPatterns class with regex pattern definitions
- `src/signal_parser/regex_parser.py` - RegexParser class with parsing and validation logic
- `tests/fixtures/sample_signals.json` - Test data with valid/invalid/edge case signal examples
- `tests/unit/test_regex_parser.py` - Comprehensive unit tests for RegexParser functionality
- `tests/unit/test_signal_patterns.py` - Unit tests for pattern matching and extraction
- `tests/unit/test_signal_validation.py` - Unit tests for signal validation logic

## QA Results

### Review Date: 2025-08-12

### Reviewed By: Quinn (Senior Developer QA)

### Code Quality Assessment

The implementation is solid and well-structured, demonstrating good understanding of regex parsing patterns and Python best practices. The developer correctly implemented all required French signal patterns, comprehensive validation logic, and proper structured logging. The test coverage is excellent with 40 tests passing across multiple test files. The code follows the project's coding standards and architecture requirements effectively.

### Refactoring Performed

- **File**: `src/signal_parser/__init__.py`
  - **Change**: Added return type annotations to `__post_init__` methods
  - **Why**: Type safety and mypy compliance
  - **How**: Ensures proper type checking and IDE support, prevents type-related bugs

- **File**: `src/signal_parser/regex_parser.py`
  - **Change**: Added return type annotation to `__init__` method
  - **Why**: Consistency and type safety
  - **How**: Improves code maintainability and static analysis

- **File**: `src/signal_parser/patterns.py`
  - **Change**: Implemented proper class-level caching for compiled regex patterns
  - **Why**: Performance optimization - regex compilation is expensive
  - **How**: Patterns are now compiled once per process and reused, reducing overhead significantly
  
- **File**: `src/signal_parser/patterns.py`
  - **Change**: Removed unused variable `_action_group` in loop
  - **Why**: Clean code principle - eliminate unused variables
  - **How**: Simplified the loop structure without affecting functionality

### Compliance Check

- Coding Standards: ✓ Follows all standards including no print() usage, structured logging, proper error handling
- Project Structure: ✓ Files correctly placed according to architecture/source-tree.md
- Testing Strategy: ✓ Comprehensive test coverage with 40 tests, using pytest framework as specified
- All ACs Met: ✓ All 6 acceptance criteria fully implemented and tested

### Improvements Checklist

[x] Added missing type annotations for better type safety
[x] Implemented pattern caching for significant performance improvement
[x] Removed unused variables and imports for cleaner code
[ ] Consider adding performance benchmarks to measure parser speed
[ ] Consider implementing a pattern validation tool to catch regex errors early
[ ] Consider adding metrics collection for parser success/failure rates

### Security Review

No security concerns identified. The implementation correctly:
- Hashes sensitive data (telegram usernames) before logging
- Uses SHA256 for text hashing to prevent message content exposure
- Validates all input data to prevent injection attacks
- No hardcoded credentials or sensitive data

### Performance Considerations

The refactored caching mechanism provides significant performance improvements:
- Regex patterns are compiled once per process lifetime instead of per parse operation
- Pattern matching uses efficient fallback strategy (specific patterns first, then component parsing)
- Decimal operations are properly handled for financial precision
- Log operations are structured for efficient parsing and aggregation

### Final Status

✓ Approved - Ready for Done

The implementation meets all requirements and has been enhanced with performance optimizations. The code is production-ready with excellent test coverage and proper error handling.