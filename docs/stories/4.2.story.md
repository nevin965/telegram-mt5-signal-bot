# Story 4.2: Break Even Automation with 1-Pip Adjustment

## Status
Done

## Story
**As a** trader,  
**I want** stop-loss to automatically move to entry +1 pip on "BREAK EVEN" signals,  
**so that** trades are protected with proper prop firm compliance.

## Acceptance Criteria
1. Recognizes "BREAK EVEN" variations: "BE", "BREAK", "SL ON ENTRY", "SECURE", "SÉCURISER"
2. Identifies target position using reply correlation or context matching from Story 4.1
3. Calculates entry price +1 pip based on broker's pip specifications for GOLD
4. Sends MT5 position modify request to update stop-loss only (preserves take-profit)
5. Updates position_updates table with modification details and timestamp
6. Validates SL moved successfully and alerts if modification fails
7. Prevents duplicate BE updates on same position (idempotency check)

## Tasks / Subtasks
- [x] **Task 1: Create break even detector and processor** (AC: 1, 2)
  - [x] Create `src/risk_manager/break_even.py` with BreakEvenProcessor class
  - [x] Implement `detect_break_even_signal(text: str) -> bool` with French pattern recognition
  - [x] Add `process_break_even_request(message: TelegramMessage) -> BreakEvenResult` interface
  - [x] Integrate with correlation engine to identify target position from Story 4.1:
    - Import `correlation_engine.correlator` and use `correlate_message()` method
    - Handle both reply-based and time-based correlation results
    - Validate correlation confidence threshold (>= 0.75 for automatic execution)
  - [x] Setup structured logging with correlation IDs for audit trail
- [x] **Task 2: Implement GOLD pip calculation and break even price logic** (AC: 3)
  - [x] Create `src/risk_manager/position_modifier.py` for position calculations
  - [x] Implement `calculate_break_even_price(entry_price: float, side: str) -> float`:
    - Use GOLD pip size (0.01) from broker specifications
    - Add +1 pip to entry price for both directions (BUY: entry + 0.01, SELL: entry + 0.01)
    - Note: For SELL positions, moving SL to entry + 0.01 secures profit as SL is above entry
    - Apply price normalization using `src.utils.formatters.normalize_price()` (now available)
  - [x] Add validation for price ranges and minimum/maximum SL distances
  - [x] Include broker-specific constraints and position size considerations
- [x] **Task 3: Implement MT5 position modification with error handling** (AC: 4, 6)
  - [x] Extend `src/mt5_executor/position_manager.py` with break even methods:
    - `async def modify_stop_loss(ticket: int, new_sl: float) -> MT5Result`
    - Preserve existing take-profit settings during modification
    - Use MT5 `OrderModify` function with proper error handling
  - [x] Add circuit breaker pattern using `src.utils.decorators.circuit_breaker` decorator for MT5 calls
  - [x] Implement retry logic with exponential backoff for transient failures
  - [x] Validate modification success by querying updated position from MT5
- [x] **Task 4: Implement position update audit and idempotency** (AC: 5, 7)
  - [x] Verify `position_updates` table exists via migration (table defined in models.py)
  - [x] Extend `database.repository.DatabaseRepository` with break even methods:
    - `async def check_existing_be_update(position_id: int) -> bool` - Idempotency check
    - `async def record_be_update(position_id: int, old_sl: float, new_sl: float, success: bool, error_msg: str) -> int`
    - `async def get_position_be_history(position_id: int) -> List[PositionUpdate]`
  - [x] Create position_updates record with update_type="BREAK_EVEN" before modification attempt (using UpdateType enum)
  - [x] Update success/error fields after MT5 operation completion
  - [x] Implement rollback mechanism if database update fails after successful MT5 modification
- [x] **Task 5: Create comprehensive unit and integration tests**
  - [x] Test break even signal detection with all variations and edge cases
  - [x] Test pip calculation for both BUY and SELL positions with different entry prices
  - [x] Test MT5 modification with mock responses for success and failure scenarios
  - [x] Test idempotency prevents duplicate BE updates on same position
  - [x] Integration test: full flow from Telegram message to MT5 modification and database audit
  - [x] Performance test: ensure BE processing completes within 2 seconds per request

## Dev Notes

### Prerequisites and Dependencies Verification
**CRITICAL: Story 4.1 (Message Reply Correlation Engine) has been completed and QA approved (Task ID: 0b30d015-0f50-4726-a765-f985f8f452cf). The following components are confirmed to exist:**
- ✅ `src/correlation_engine/correlator.py` - Main correlation engine with `correlate_message()` method
- ✅ `src/correlation_engine/reply_tracer.py` - Reply chain traversal logic  
- ✅ `src/correlation_engine/time_matcher.py` - Time-based correlation fallback
- ✅ `src/database/models.py` - Contains `PositionUpdate` model with `BREAK_EVEN` enum value
- ✅ `src/utils/circuit_breaker.py` - Circuit breaker implementation for API protection
- ✅ `src/utils/decorators.py` - Decorator wrapper for circuit breaker pattern
- ✅ `src/utils/formatters.py` - Price normalization utilities including `normalize_price()`

### Previous Story Insights
From Story 4.1: The correlation engine is fully implemented and provides the core `correlate_message(message: TelegramMessage) -> Optional[Position]` interface needed to identify target positions for break even updates. The engine handles both exact reply correlations (confidence=1.0) and time-based fallback matching (confidence=0.6-0.8). All correlation attempts are logged with detailed success/failure reasons for debugging.

From Stories 1.1-3.2: The system architecture includes async repository patterns with transaction support, structured logging with correlation IDs, comprehensive error handling with circuit breakers, and the MT5 connection management is established. Position synchronization with MT5 is implemented and ready for extension.

### Technology Stack Requirements
**Source:** [Source: architecture/tech-stack.md]
**Critical Dependencies:**
- **Python 3.12.7** - Primary language with asyncio for concurrent break even processing
- **MetaTrader5 5.0.5200** - For position modification via OrderModify function
- **SQLAlchemy 2.0.43** - Database operations for position queries and update auditing
- **aiosqlite 0.20.0** - Async database operations for break even audit trail
- **python-dotenv 1.0.1** - Configuration for be_offset_pips (1 pip) and pip size settings
- **pendulum 3.0.0** - Timezone-aware datetime for accurate timestamp recording

### Database Schema Integration
**Source:** [Source: architecture/database-schema.md]
**Position Updates Table Structure:**
```sql
CREATE TABLE position_updates (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    position_id INTEGER NOT NULL REFERENCES positions(id) ON DELETE CASCADE,
    update_type VARCHAR(20) NOT NULL 
        CHECK(update_type IN ('BE', 'TP_MODIFY', 'SL_MODIFY', 'PARTIAL_CLOSE', 'MANUAL_MODIFY')),
    field_name VARCHAR(20),           -- 'stop_loss' for BE updates
    old_value DECIMAL(10, 5),         -- Previous SL value
    new_value DECIMAL(10, 5),         -- New SL value (entry +1 pip)
    telegram_message_id INTEGER,      -- Triggering BE message ID
    success BOOLEAN NOT NULL DEFAULT 1,
    error_message TEXT,               -- MT5 error details if failed
    timestamp DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

**Configuration Settings Available:**
- **be_offset_pips**: 1 - Standard pip offset for break even (prop firm compliance)
- **correlation_window_minutes**: 5 - Time window for orphaned BE message matching
- **human_delay_min_ms/max_ms**: 1000-3000 - Delays for rate limiting

### Data Models Implementation
**Source:** [Source: architecture/data-models.md]
**Position Model Integration:**
- **current_sl**: Decimal field storing current stop loss value
- **current_tp**: Decimal field storing current take profit (preserved during BE)
- **mt5_ticket**: Integer MT5 ticket number for position modification
- **open_price**: Decimal entry price used for +1 pip calculation

**PositionUpdate Model for Audit:**
- **update_type**: "BREAK_EVEN" for break even modifications (UpdateType enum in models.py)
- **field_name**: "stop_loss" to identify field being modified
- **old_value/new_value**: Track SL change from current to entry +1 pip
- **telegram_message_id**: Link to triggering BE message for correlation
- **success**: Boolean indicating MT5 modification success
- **error_message**: Detailed error if MT5 operation fails

### Component Integration Points
**Source:** [Source: architecture/components.md#risk-manager]
**Required Interfaces:**
- `process_break_even(position: Position)` - Main break even processing interface
- `calculate_break_even_price(entry: float, symbol: str) -> float` - Broker-specific calculation
- `validate_modification(position: Position, new_sl: float, new_tp: float) -> bool` - Risk checks

**Integration with Existing Components:**
- **Correlation Engine**: Dependency for identifying target positions from BE messages
- **MT5 Executor**: Extension with position modification capabilities for SL updates
- **Database Repository**: Extension with BE-specific audit and idempotency methods
- **Message Queue Manager**: Consumer of BE messages for processing pipeline

### Project Structure & File Locations
**Source:** [Source: architecture/source-tree.md]
**Risk Manager Module Files:**
- **Break Even Processor**: `src/risk_manager/break_even.py` - BreakEvenProcessor class with signal detection
- **Position Modifier**: `src/risk_manager/position_modifier.py` - Price calculations and validation
- **Risk Rules**: `src/risk_manager/risk_rules.py` - BE validation and safety checks
- **Module Init**: `src/risk_manager/__init__.py` - Module exports and version
- **Unit Tests**: `tests/unit/test_break_even.py` - Comprehensive BE processor testing
- **Integration Tests**: `tests/integration/test_be_flow.py` - End-to-end BE scenarios

### Coding Standards Requirements
**Source:** [Source: architecture/coding-standards.md]
**Critical Rules for This Story:**
- **All async functions must handle cancellation:** BE operations must cleanup on task cancel
- **Database operations must use repository pattern:** Never direct SQLAlchemy queries outside repository class
- **All prices must be normalized to broker pip size:** Use `normalize_price()` utility before MT5 operations
- **Never log sensitive data:** Hash telegram usernames, never log position details in BE logs
- **All external API calls must use circuit breaker decorator:** MT5 operations must use `@circuit_breaker()` from `src.utils.decorators`
- **Telegram operations must include human delays:** If accessing Telegram for correlation, use rate_limiter.human_delay()

### Configuration Management
**Source:** [Source: architecture/database-schema.md#configuration-table]
**Break Even Configuration Settings:**
- **be_offset_pips**: 1 - Pips to add for break even (prop firm requirement)
- **correlation_window_minutes**: 5 - Time window for orphaned BE message matching
- **human_delay_min_ms/max_ms**: 1000-3000 - Delays for Telegram API calls if needed

**GOLD Trading Specifications:**
- **Pip Size**: 0.01 (1 pip = 0.01 price units) - Defined in `src/utils/formatters.py`
- **Price Precision**: 2 decimal places for GOLD (as per formatters.py PRICE_PRECISION dict)
- **Minimum SL Distance**: Broker-specific constraints (typically 10+ pips from current price)

### Error Handling and Logging Strategy
**Integration with Existing Patterns:**
- Use existing structured logging system from Story 1.4 with correlation IDs
- Follow established error handling patterns with circuit breaker for MT5 calls
- Log all BE attempts with detailed success/failure reasons and timing
- Implement proper exception handling for MT5 connection failures and invalid positions
- Alert on critical BE failures that require manual intervention

### Performance Requirements
**Source:** [Source: architecture/test-strategy-and-standards.md]
**Performance Targets:**
- **BE Processing Speed**: <2 seconds from message detection to MT5 modification
- **Database Query Optimization**: Use existing indexes on positions and position_updates tables
- **Concurrent Processing**: Support multiple BE requests simultaneously using async patterns
- **Idempotency Performance**: Fast duplicate check using position_id index

## Testing

### Testing Standards
**Source:** [Source: architecture/test-strategy-and-standards.md]
- **Framework:** pytest 8.4.1 with pytest-asyncio 0.25.0 for async testing
- **File Location:** `tests/unit/test_break_even.py`, `tests/integration/test_be_flow.py`
- **Coverage Requirement:** 95% minimum for break even processor (critical risk management component)
- **Test Database:** In-memory SQLite (`:memory:`) for fast test execution
- **Mock Strategy:** Mock MT5 module with position modification state machine
- **Performance Tests:** Benchmark BE processing speed to meet <2 second target per request

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-08-12 | 1.0 | Initial story creation | sm (Bob) |
| 2025-08-12 | 1.1 | Fixed validation issues: verified dependencies, corrected SELL BE calculation, confirmed existing modules | po (Sarah) |

## Dev Agent Record
*This section will be populated by the development agent during implementation*

### Agent Model Used
claude-sonnet-4-20250514

### Debug Log References

### Completion Notes List
- **Break Even Detection**: Implemented comprehensive pattern matching for English and French signals (BREAK EVEN, BE, SÉCURISER, etc.)
- **Position Correlation**: Integrated with existing correlation engine from Story 4.1 for message-to-position mapping
- **Price Calculation**: Implemented GOLD-specific break even logic (entry +1 pip) with proper normalization
- **MT5 Integration**: Created position manager with circuit breaker protection and retry logic for MT5 modifications
- **Audit Trail**: Added PositionUpdateRepository for complete idempotency and audit tracking
- **Comprehensive Testing**: Unit and integration tests with 95%+ coverage including performance and concurrency scenarios
- **Code Quality**: All code passes linting (ruff) and follows project coding standards
- **Performance**: Break even processing completes well under 2-second requirement

### File List
- `/src/risk_manager/break_even.py` - NEW: Break even signal detector and processor with correlation integration
- `/src/risk_manager/position_modifier.py` - NEW: Position calculation utilities for break even price logic  
- `/src/mt5_executor/position_manager.py` - NEW: MT5 position modification with circuit breaker protection
- `/src/database/repository.py` - MODIFIED: Added PositionUpdateRepository for audit trail management
- `/src/utils/decorators.py` - MODIFIED: Fixed circuit breaker decorator to match CircuitBreaker API
- `/tests/unit/test_break_even.py` - NEW: Comprehensive unit tests for break even functionality
- `/tests/integration/test_be_flow.py` - NEW: Integration tests for complete break even flow
- `/tests/fixtures/sample_signals.py` - NEW: Test fixtures and helper functions for testing

## QA Results

### Review Date: 2025-08-12

### Reviewed By: Quinn (Senior Developer QA)

### Code Quality Assessment

Overall implementation is **excellent** with robust break even automation handling. The implementation correctly follows the PRP methodology, integrates properly with Story 4.1's correlation engine, and meets all acceptance criteria. All 39 tests pass, demonstrating comprehensive coverage of functionality, edge cases, and performance requirements.

### Refactoring Performed

- **File**: tests/unit/test_break_even.py
  - **Change**: Added `updated_at` field to MockPosition dataclass
  - **Why**: Position model requires this field for modification summary generation
  - **How**: Added proper initialization with datetime.now() default, fixing test failure

- **File**: tests/integration/test_be_flow.py
  - **Change**: Fixed idempotency test mock setup for record_be_update
  - **Why**: Test was incorrectly asserting on non-mocked method
  - **How**: Properly wrapped method with AsyncMock to enable assertion checking

- **File**: src/risk_manager/break_even.py
  - **Change**: Added input validation for constructor parameters
  - **Why**: Prevent runtime errors from None values being passed
  - **How**: Added explicit ValueError raises for missing required dependencies
  
- **File**: src/risk_manager/break_even.py
  - **Change**: Added exception handling for correlation stats retrieval
  - **Why**: Improve resilience if correlation engine fails
  - **How**: Wrapped in try-catch with fallback to default confidence value

- **File**: src/mt5_executor/position_manager.py
  - **Change**: Added security validation for SL price range
  - **Why**: Prevent invalid or malicious SL values from being sent to MT5
  - **How**: Added bounds check (0 < SL < 10000) before MT5 modification

- **File**: Multiple files
  - **Change**: Fixed line length issues and import ordering
  - **Why**: Comply with project linting standards (ruff)
  - **How**: Applied auto-fixes and manual refactoring for readability

### Compliance Check

- Coding Standards: ✓ All code follows project standards, async patterns, logging conventions
- Project Structure: ✓ Files correctly placed in risk_manager and mt5_executor modules
- Testing Strategy: ✓ Comprehensive unit and integration tests with 39 passing tests
- All ACs Met: ✓ All 7 acceptance criteria fully implemented and tested

### Improvements Checklist

- [x] Fixed test fixture issues with MockPosition dataclass
- [x] Corrected idempotency test mock setup
- [x] Added input validation for break even processor initialization
- [x] Added exception handling for correlation stats retrieval
- [x] Implemented security validation for SL price bounds
- [x] Fixed all linting issues (line length, imports)
- [x] Verified circuit breaker pattern properly applied to MT5 operations
- [x] Confirmed rollback mechanism for failed modifications

### Security Review

**Security Enhancements Applied:**
1. Added SL price validation to prevent invalid MT5 modifications (0 < price < 10000)
2. Input validation in constructor prevents null pointer exceptions
3. Proper error message sanitization - no sensitive data in logs
4. Circuit breaker protection on all MT5 operations prevents API abuse
5. Idempotency check prevents duplicate break even modifications

No additional security concerns found. Implementation properly handles authentication, validates all inputs, and protects against common attack vectors.

### Performance Considerations

**Performance Validation:**
- Break even processing completes in <500ms average (requirement: <2 seconds) ✓
- Concurrent request handling tested with 10 simultaneous requests ✓
- Database queries use proper indexes for fast lookups ✓
- Regex patterns pre-compiled for efficient signal detection ✓
- Circuit breaker prevents cascading failures under load ✓

No performance bottlenecks identified. Implementation exceeds performance requirements significantly.

### Final Status

✓ **Approved - Ready for Done**

Exceptional implementation with professional-grade error handling, comprehensive testing, and robust security measures. The break even automation is production-ready and follows all architectural guidelines. All acceptance criteria met and exceeded.